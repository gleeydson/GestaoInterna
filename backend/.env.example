# Copie este arquivo para .env e ajuste os valores conforme necessário

# Ambiente de execução (development ou production)
NODE_ENV=development

# Porta do servidor
PORT=3000

# Banco de dados SQLite
# Em desenvolvimento: ./epi_control_dev.db
# Em produção: ./epi_control_prod.db
DB_PATH=./epi_control_dev.db

# JWT Secret - GERAR UMA CHAVE FORTE EM PRODUÇÃO!
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=trocar-esta-chave-secreta-em-producao

# CORS Origins (separados por vírgula)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
